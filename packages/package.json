{
  "name": "bunnel-monorepo",
  "private": true,
  "workspaces": [
    "bunnel",
    "bunnel-server"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "rm -rf */dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
